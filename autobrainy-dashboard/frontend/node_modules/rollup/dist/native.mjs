let parse, parseAsync, xxhashBase64Url, xxhashBase36, xxhashBase16;
try {
  // Prefer the wasm-node bundle's native helper when available
  const m = await import('@rollup/wasm-node/dist/native.js');
  ({ parse, parseAsync, xxhashBase64Url, xxhashBase36, xxhashBase16 } = m);
} catch (e1) {
  try {
    const c = await import('@rollup/wasm-node');
    ({ parse, parseAsync, xxhashBase64Url, xxhashBase36, xxhashBase16 } = c.default || c);
  } catch (e2) {
    const c2 = await import('./native.js');
    const ns = c2.default || c2;
    ({ parse, parseAsync, xxhashBase64Url, xxhashBase36, xxhashBase16 } = ns);
  }
}
export { parse, parseAsync, xxhashBase64Url, xxhashBase36, xxhashBase16 };
